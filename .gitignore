# ── Secrets — never commit these ─────────────────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
secrets/
credentials/

# ── Database — lives on the data branch only, never on main ──────────────────
flights.db
*.db-journal
*.db-wal
*.db-shm

# ── Generated site — managed by dashboard workflow on gh-pages branch ─────────
site/

# ── FAA registry downloads — fetched transiently by enrich.py ────────────────
ReleasableAircraft.zip
MASTER.txt
ACFTREF.txt

# ── Python artifacts ──────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/
.pytest_cache/
htmlcov/
.coverage

# ── OS / editor ───────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
